query getTasksPagination($page: Int!) {
  tasksPagination(page: $page){
    tasks{
      id
      name
      description
      status
    }
    pagination{
      current,
      totalPages
    }
  }
}
